


We can define per-page macro files.

We can define inclusion macro files.

Inclusion macro files reside preferably in the Template namespace.

<amsmath i="Macrofilename">  includes the page Macrofilename and there the portion between <macro>...</macro> ???

<amsmath i="Macrofilename#SUBNAME">  and there only section SUBNAME ???




<macro>...</macro> can be on every page and defines a page local macro.


<amsmath out>   causes writeout of relevant continuation parameters to be written out

<amsmath in>  causes the earlier continuation parameters to be read in

<amsmath inout>  causes them to be read in and written out





